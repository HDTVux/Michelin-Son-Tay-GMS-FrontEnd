# Biến build cho môi trường production (Vite tự nạp khi chạy `vite build`).
# VAPID PUBLIC KEY là khoá công khai -> an toàn để commit.
# PHẢI trùng đúng public key mà backend production đang dùng (VAPID_PUBLIC_KEY).
VITE_VAPID_PUBLIC_KEY=BG1kuwtYSEvh_4bfD4YIzP_EgYrjaO26UdX-3NWGeCKAX0lkbl9K-aFH-p2qg80fgo2JH49bdu4WbL8_zK_Irj8

# Nếu production gọi API qua domain riêng, đặt luôn ở đây (bỏ trống sẽ dùng mặc định trong code):
# VITE_API_BASE_URL=https://api.sontaygarage.vn
