const fs=require("fs");const f="src/pages/Technician/ServiceTicket/ServiceTicket.jsx";let c=fs.readFileSync(f,"utf8");let lines=c.split("\n");const F={154:"          toast.error(\"Vui long dang nhap\");"};let n=0;for(const[num,fix]of Object.entries(F)){const i=parseInt(num)-1;if(i>=0&&i<lines.length){lines[i]=fix;n++}};fs.writeFileSync(f,lines.join("\n"),"utf8");console.log("Fixed:",n)
